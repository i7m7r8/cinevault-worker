- name: Build & Deploy Worker
  run: |
    cargo install worker-build --version 0.6.0
    worker-build --release
  env:
    CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
    CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
